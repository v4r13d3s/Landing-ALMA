<section id="contact" class="family-theme">
  <div class="contact-wave-top">
    <svg viewBox="0 0 1440 100" preserveAspectRatio="none">
      <path fill="#ffffff" d="M0,32L120,42.7C240,53,480,75,720,74.7C960,75,1200,53,1320,42.7L1440,32L1440,100L1320,100C1200,100,960,100,720,100C480,100,240,100,120,100L0,100Z"></path>
    </svg>
  </div>

  <div class="full-header-wrapper">
    <div class="heart-container">
      <svg class="heart-svg" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
        <path d="M12,21.35L10.55,20.03C5.4,15.36 2,12.27 2,8.5C2,5.41 4.41,3 7.5,3C9.24,3 10.91,3.81 12,5.08C13.09,3.81 14.76,3 16.5,3C19.58,3 22,5.41 22,8.5C22,12.27 18.6,15.36 13.45,20.03L12,21.35Z"></path>
      </svg>
    </div>
    <h2>¡Queremos conocerte!</h2>
    <p>Estamos aquí para escucharte y acompañarte en el proceso de tu pequeño.</p>
  </div>

  <div class="container-wrapper">
    <div class="main-contact-layout">
      
      <div class="form-column">
        <form class="family-form">
          <div class="form-body">
<div class="input-field">
  <label>Tu nombre completo</label>
  <input 
    type="text" 
    placeholder="Escribe tu nombre" 
    required 
    maxlength="50"
    pattern="[A-Za-zÁéíóúÁÉÍÓÚñÑ ]+"
    title="Por favor, ingresa solo letras (máximo 50 caracteres)"
  />
</div>
<div class="input-field">
  <label>Tu correo electrónico</label>
  <input 
    type="email" 
    placeholder="ejemplo@correo.com" 
    required 
    maxlength="254"
    pattern="[a-z0-9._%+-]+@[a-z0-9.-]+\.[a-z]{2,}$"
    title="Por favor, ingresa un correo electrónico válido"
  />
</div>
            <div class="input-field">
              <label>¿En qué podemos ayudarte?</label>
              <textarea rows="5" maxlength="254" placeholder="Cuéntanos un poco más..." required></textarea>
            </div>
          </div>

          <button type="submit" class="btn-smart-moth">
            <div class="svg-wrapper-1">
              <div class="svg-wrapper">
                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" width="24" height="24">
                  <path fill="none" d="M0 0h24v24H0z"></path>
                  <path fill="currentColor" d="M1.946 9.315c-.522-.174-.527-.455.01-.634l19.087-6.362c.529-.176.832.12.684.638l-5.454 19.086c-.15.529-.455.547-.679.045L12 14l6-8-8 6-6.054-2.685z"></path>
                </svg>
              </div>
            </div>
            <span class="btn-text">ENVIAR MENSAJE</span>
          </button>
        </form>
      </div>

      <div class="sidebar-column">
        <div class="help-card fresh-wombat">
          <div class="bg-glow"></div>
          <div class="content-wrapper">
            <div class="top-content">
              <h3>Asistencia Directa</h3>
              <p class="subtitle">Canales directos para una respuesta inmediata:</p>
              
              <ul class="contact-list">
                <li class="list-item">
                  <div class="icon-circle yellow-bg">
                    <i class="fa fa-whatsapp"></i>
                  </div>
                  <div class="text-info">
                    <span>WhatsApp</span>
                    <a href="https://wa.me/12125552368" target="_blank">(212) 555-2368</a>
                  </div>
                </li>
                <li class="list-item">
                  <div class="icon-circle blue-bg">
                    <i class="fa fa-envelope-o"></i>
                  </div>
                  <div class="text-info">
                    <span>Correo Soporte</span>
                    <a href="mailto:alma@gmail.com">alma@gmail.com</a>
                  </div>
                </li>
              </ul>
            </div>
            

          </div>
        </div>
      </div>

    </div>
  </div>
</section>

<style>
  /* --- Estructura Base --- */
  .family-theme {
    background-color: #f0f7ff;
    padding-bottom: 120px;
    position: relative;
    overflow-x: hidden;
    font-family: 'Fredoka', sans-serif;
  }

  /* --- Encabezado Centrado --- */
  .full-header-wrapper {
    width: 100%;
    text-align: center;
    margin-bottom: 50px;
    padding-top: 0px;
  }
  .full-header-wrapper h2 { color: #2b6cb0; font-size: 3rem; font-weight: 800; margin: 10px 0; }
  .full-header-wrapper p { color: #000000; font-size: 1.1rem; }

  .heart-container { display: flex; justify-content: center; }
  .heart-svg { width: 80px; height: 80px; fill: #ef4444; animation: pulse 1.5s infinite; }
  @keyframes pulse { 0% { transform: scale(1); } 15% { transform: scale(1.2); } 30% { transform: scale(1); } }

  /* --- Layout --- */
  .container-wrapper { max-width: 1000px; margin: 0 auto; padding: 0 20px; }
  .main-contact-layout { display: flex; align-items: stretch; gap: 40px; position: relative; }
  .form-column { flex: 1; display: flex; }
  .sidebar-column { flex: 0 0 380px; margin-right: -140px; display: flex; z-index: 10; }

  /* --- Formulario --- */
  .family-form {
    width: 100%; background: white; padding: 45px; border-radius: 40px;
    border: 3px solid #2f709f; box-shadow: 0 20px 40px rgba(43, 108, 176, 0.08);
    display: flex; flex-direction: column; justify-content: space-between;
  }
  .input-field { margin-bottom: 20px; }
  .input-field label { display: block; color: #194575; font-weight: 700; margin-bottom: 8px; }
  .input-field input, .input-field textarea {
    width: 100%; padding: 16px; border: 2px solid #b5c5da; border-radius: 20px; background: #f8fafc;
  }

  /* --- BOTÓN SMART MOTH (CORREGIDO) --- */
  .btn-smart-moth {
    background: #1e4b8a;
    color: white;
    border: none;
    border-radius: 20px;
    height: 64px;
    width: 100%;
    position: relative;
    overflow: hidden;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.3s ease;
  }

  .btn-text {
    font-weight: 600;
    font-size: 18px;
    letter-spacing: 1px;
    display: block;
    transition: transform 0.5s cubic-bezier(0.76, 0, 0.24, 1);
    z-index: 1;
  }

  .svg-wrapper-1 {
    position: absolute;
    /* Centrado inicial fuera de vista a la izquierda */
    left: 50%;
    top: 50%;
    transform: translate(-520%, -50%); 
    transition: transform 0.5s cubic-bezier(0.76, 0, 0.24, 1);
    z-index: 2;
  }

  /* HOVER */
  .btn-smart-moth:hover { background: #153a6d; }

  .btn-smart-moth:hover .btn-text {
    transform: translateX(600px); /* El texto sale totalmente por la derecha */
  }

  .btn-smart-moth:hover .svg-wrapper-1 {
    transform: translate(-50%, -50%) scale(1.3); /* El avión llega al centro exacto */
  }

  .btn-smart-moth:hover .svg-wrapper {
    animation: fly-1 0.6s ease-in-out infinite alternate;
  }

  @keyframes fly-1 { from { transform: translateY(0.1em); } to { transform: translateY(-0.1em); } }

  /* --- Tarjeta Asistencia Directa (Restaurada) --- */
  .help-card.fresh-wombat {
    position: relative; width: 100%; height: 100%; border-radius: 40px;
    padding: 4px; background: linear-gradient(135deg, #3b82f6 0%, #fbbf24 100%);
    overflow: hidden; display: flex; box-shadow: -15px 20px 50px rgba(0,0,0,0.1);
  }
  .bg-glow {
    position: absolute; width: 200%; height: 200%;
    background: conic-gradient(from 0deg, transparent 0 330deg, #fbbf24 350deg, #ffffff 360deg);
    animation: rotate 4s linear infinite; top: -50%; left: -50%;
  }
  @keyframes rotate { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }

  .content-wrapper {
    position: relative; z-index: 2; background: #ffffff; width: 100%; height: 100%;
    border-radius: 36px; padding: 40px; display: flex; flex-direction: column; justify-content: space-between;
  }
  .content-wrapper h3 { color: #1a365d; font-size: 1.8rem; font-weight: 800; margin-bottom: 10px; }
  
  .contact-list { list-style: none; padding: 0; margin-top: 20px; }
  .list-item {
    display: flex; align-items: center; gap: 15px; margin-bottom: 25px;
    padding: 12px; border-radius: 20px; border: 1px solid #f1f5f9;
  }
  .icon-circle {
    width: 50px; height: 50px; border-radius: 50%;
    display: flex; align-items: center; justify-content: center;
    color: white; font-size: 1.3rem;
  }
  .yellow-bg { background: #eab308; box-shadow: 0 4px 10px rgba(234, 179, 8, 0.2); }
  .blue-bg { background: #3b82f6; box-shadow: 0 4px 10px rgba(59, 130, 246, 0.2); }
  
  .text-info span { font-size: 0.75rem; text-transform: uppercase; color: #000000; font-weight: 700; }
  .text-info a { display: block; color: #496188; font-weight: 700; text-decoration: none; font-size: 1.1rem; }
  
  .community-tag { text-align: center; color: #2d415c; padding-top: 20px; border-top: 1px dashed #e2e8f0; }

  /* Responsivo */
  @media (max-width: 1200px) { .sidebar-column { margin-right: 0; } }
  @media (max-width: 900px) {
    .main-contact-layout { flex-direction: column; }
    .sidebar-column { flex: 1; width: 100%; }
    .full-header-wrapper h2 { font-size: 2.3rem; }
  }
</style>