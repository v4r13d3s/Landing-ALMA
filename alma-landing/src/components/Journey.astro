<section id="journey" class="journey-section">
  <div class="bg-pattern"></div>

  <div class="main-container">
    <div class="header-content">
      <h2>Tu camino con <span class="blue">ALMA</span></h2>
      <p>Pasos sencillos diseñados para disfrutar el aprendizaje en familia.</p>
    </div>

    <div class="map-wrapper">
      <div class="orbit orbit-1"></div>
      <div class="orbit orbit-2"></div>
      <div class="orbit orbit-3"></div>

      <div class="step-box p1">
        <div class="witty-card border-yellow">
          <div class="card-inner">
            <div class="num-tag yellow-bg">1</div>
            <h3>Perfil Personalizado</h3>
            <p>Entendemos las necesidades únicas de tu pequeño para cada juego.</p>
          </div>
        </div>
      </div>

      <div class="step-box p2">
        <div class="witty-card border-blue">
          <div class="card-inner">
            <div class="num-tag blue-bg">2</div>
            <h3>Actividades Diarias</h3>
            <p>Biblioteca de juegos basados en estimulación cognitiva y motriz.</p>
          </div>
        </div>
      </div>

      <div class="step-box p3">
        <div class="witty-card border-yellow">
          <div class="card-inner">
            <div class="num-tag yellow-bg">3</div>
            <h3>Logros Compartidos</h3>
            <p>Visualiza el progreso real y celebra cada hito en familia.</p>
          </div>
        </div>
      </div>

      <div class="download-center">
        <h4>Comienza la aventura hoy</h4>
        <div class="app-grid">
          <a href="#" class="app-link dark-btn">
             <i class="fa fa-apple"></i> App Store
          </a>
          <a href="#" class="app-link dark-btn">
             <i class="fa fa-play"></i> Google Play
          </a>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .journey-section {
    position: relative;
    padding: 120px 20px;
    background-color: #f0f7ff;
    overflow: hidden;
    min-height: 950px;
    font-family: 'Fredoka', sans-serif;
  }

  /* Patrón de puntos optimizado */
  .bg-pattern {
    position: absolute;
    inset: 0;
    background-image: radial-gradient(#2b6cb0 0.8px, transparent 0.8px);
    background-size: 40px 40px;
    opacity: 0.15;
    pointer-events: none;
  }

  .main-container {
    max-width: 1200px;
    margin: 0 auto;
    position: relative;
    z-index: 2;
  }

  .header-content { text-align: center; margin-bottom: 70px; }
  .badge { 
    background: #fff; 
    border: 1.5px solid #e2e8f0; 
    padding: 6px 18px; 
    border-radius: 20px; 
    font-size: 0.85rem; 
    font-weight: 700;
    color: #4a5568;
    box-shadow: 0 4px 6px rgba(0,0,0,0.02);
  }
  
  .header-content h2 { font-size: 3.2rem; color: #1a365d; margin: 20px 0; font-weight: 800; }
  .blue { color: #2b6cb0; }
  .header-content p { color: #0e1217; font-size: 1.2rem; }

  /* Estructura del Mapa */
  .map-wrapper {
    position: relative;
    height: 750px;
    display: flex;
    justify-content: center;
    align-items: center;
  }

  /* Círculos de órbita con grosor coherente */
  .orbit {
    position: absolute;
    border: 3px solid #5b7593; /* Grosor aumentado */
    border-radius: 50%;
    top: 50%; left: 50%;
    transform: translate(-50%, -50%);
    transition: border-color 0.3s ease;
  }
  .orbit-1 { width: 380px; height: 380px; }
  .orbit-2 { width: 600px; height: 600px; }
  .orbit-3 { width: 850px; height: 850px; }

  /* Tarjetas Elevadas */
  .step-box {
    position: absolute;
    width: 300px;
    z-index: 10;
    transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
  }

  .p1 { top: 2%; right: 2%; }
  .p2 { top: 35%; left: -2%; }
  .p3 { bottom: 5%; right: 2%; }

  .witty-card {
    background: white;
    padding: 30px;
    border-radius: 30px; /* Redondeo unificado */
    box-shadow: 0 20px 40px rgba(43, 108, 176, 0.12); /* Sombra azulada */
    border-bottom: 6px solid transparent; /* Efecto de profundidad */
    transition: all 0.3s ease;
  }

  .border-yellow { border: 3px solid #fbbf24; border-bottom: 8px solid #fbbf24; }
  .border-blue { border: 3px solid #3b82f6; border-bottom: 8px solid #3b82f6; }

  /* Interacción Hover */
  .step-box:hover {
    transform: translateY(-10px) scale(1.02);
  }
  
  .step-box:hover .witty-card {
    box-shadow: 0 30px 50px rgba(43, 108, 176, 0.2);
  }

  .num-tag {
    width: 40px; height: 40px; border-radius: 50%;
    display: flex; align-items: center; justify-content: center;
    color: white; font-weight: 900; margin-bottom: 20px;
    font-size: 1.1rem;
    box-shadow: 0 4px 10px rgba(0,0,0,0.1);
  }
  .yellow-bg { background: #fbbf24; }
  .blue-bg { background: #3b82f6; }

  .card-inner h3 { font-size: 1.4rem; margin-bottom: 10px; color: #1a365d; font-weight: 800; }
  .card-inner p { font-size: 1rem; color: #000000; line-height: 1.5; }

  /* Caja Central de Descarga */
  .download-center {
    background: white;
    padding: 40px;
    border-radius: 40px;
    box-shadow: 0 25px 60px rgba(43, 108, 176, 0.15);
    text-align: center;
    width: 320px;
    z-index: 20;
    border: 2px solid #253b52;
  }

  .download-center h4 { 
    margin-bottom: 25px; 
    color: #022c66; 
    font-weight: 800; 
    font-size: 1.1rem;
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }

  .app-grid { display: flex; flex-direction: column; gap: 15px; }

  .dark-btn {
    background: #0f172a; 
    color: white; 
    padding: 14px;
    border-radius: 16px; 
    text-decoration: none; 
    font-weight: 700;
    font-size: 1rem;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
    transition: all 0.3s ease;
  }

  .dark-btn:hover {
    background: #1e293b;
    transform: translateY(-3px);
    box-shadow: 0 10px 20px rgba(0,0,0,0.2);
  }

  .footer-note { margin-top: 15px; color: #323b48; font-size: 0.85rem; font-weight: 600; }

  /* Responsivo */
  @media (max-width: 900px) {
    .journey-section { padding: 60px 20px; }
    .map-wrapper { height: auto; flex-direction: column; gap: 30px; }
    .orbit, .bg-pattern { display: none; }
    .step-box { position: relative; inset: auto; width: 100%; max-width: 400px; }
    .header-content h2 { font-size: 2.4rem; }
  }
</style>